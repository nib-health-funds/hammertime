'use strict';

const assert          = require('assert');
const startHammertime = require('../start-hammertime');
const AWS             = require('aws-sdk-mock');

describe('start-hammertime', () => {

  describe('startEC2()', () => {

    xit('should start stopped, tagged instances', () => {

    });

  });

});
